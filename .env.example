# Copy to .env and fill in. .env is git-ignored; never commit real credentials.
#
# This is one of three credential sources. You can instead pass
# `--connection NAME` to use an entry from Snowflake's connections.toml, and
# anything left unset here is prompted for at runtime.

# Required

SNOWFLAKE_ACCOUNT=myorg-myaccount
SNOWFLAKE_USER=me

# One credential is required: a password, or SSO via the authenticator.
SNOWFLAKE_PASSWORD=
# SNOWFLAKE_AUTHENTICATOR=externalbrowser

# Role used for the run and the ACCOUNT_USAGE report (needs ACCOUNT_USAGE access).
SNOWFLAKE_ROLE=ACCOUNTADMIN
